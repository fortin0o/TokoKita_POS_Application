<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AplikasiKedua" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_brand</item>
        <item name="android:statusBarColor">@color/primary_brand</item>
        <item name="android:windowBackground">@color/background_light</item>
    </style>

    <style name="Theme.AplikasiKedua" parent="Base.Theme.AplikasiKedua" />
</resources>